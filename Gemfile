source "https://rubygems.org"

# GitHub Pages builds this site with the pinned github-pages gem set.
# Using it locally keeps `jekyll serve` consistent with production.
gem "github-pages", group: :jekyll_plugins

# Ruby 3+ no longer bundles webrick, which `jekyll serve` needs locally.
gem "webrick", "~> 1.8"
